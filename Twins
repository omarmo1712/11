#include <iostream>
#include <vector> 

using namespace std;
int main()
{
	int n = 0;
	int m = 0;
	int counter = 0;
	cin >> n >> m;
	for (size_t i = 1; i > 0; i++)
	{
		if (i %m == 0)
		{
			n++;
		}
		n--;
		if (n <= 0)
		{
			cout << i << endl;
			break;
		}
	}
}
